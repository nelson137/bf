mod(interpreter)
    rename -> runtime
        gets rid of #[allow(clippy::module_inception)]
    prevent complex logical infinite loops
        execute code in another thread
        support execution timeout
subcmd(live)
    support program input
        key-bind for PromptStrDialogue
        allow defining default byte to use if no input available
    support resizing code & output by dragging border
    support scrolling in editor and output
    re-bind keys ??
